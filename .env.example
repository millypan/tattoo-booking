# 這份檔案是「範例」，告訴你這個網站需要哪些設定值、以及去哪裡拿。
# 正式使用時，請新建一份叫做 .env.local 的檔案（本機開發用）或到 Vercel 專案的
# Settings → Environment Variables 頁面（正式上線用），把下面每一項換成真實的值。
# 千萬不要把真的 token／id 貼到這份 .env.example 裡、也不要把它以外的 .env.local 上傳到網路上。

# --- Notion 連線設定 ---

# Notion internal integration 的 token，用來讓網站程式讀寫 Notion 資料庫。
# 拿法：登入 Notion → 打開 https://www.notion.so/my-integrations → 建立一個新的
# integration（或選現有的）→ 複製「Internal Integration Secret」。
# 建立/選好 integration 後，記得回到每一個要用到的 Notion 頁面，右上角「⋯」→
# 「Connections」→ 把這個 integration 加進去，不然網站會讀不到資料（常見錯誤原因）。
NOTION_TOKEN=ntn_範例token字串請換成你自己的

# --- 各資料庫的 id（每個資料庫網址列的最後一段亂碼就是 id）---

# 作品資料庫：刺青作品清單（圖片、風格、價格、狀態等）。
WORKS_DB=00000000-0000-0000-0000-000000000001

# 時段資料庫：可預約的時間段。
SLOTS_DB=00000000-0000-0000-0000-000000000002

# 預約訂單資料庫：客人送出的預約單（認領圖／客製圖）。
BOOKINGS_DB=00000000-0000-0000-0000-000000000003

# 系列資料庫：作品所屬的系列分類。
SERIES_DB=00000000-0000-0000-0000-000000000004

# 客戶資料庫：客人的稱呼、標籤等基本資料。
CUSTOMERS_DB=00000000-0000-0000-0000-000000000005

# 系統設定資料庫：存放後台密碼等系統設定值（讓你可以直接在 Notion 裡改密碼，不用改程式）。
SETTINGS_DB=00000000-0000-0000-0000-000000000006

# --- 後台備援密碼 ---

# 後台密碼的「正本」其實存在上面的「系統設定」資料庫裡（項目＝「後台密碼」那一列）。
# 這個環境變數只是「備援」：萬一 Notion 那筆設定讀不到（例如被誤刪、Notion 暫時打不開），
# 網站會自動改用這裡的值當後台密碼，避免被鎖在後台外面。平常不用特別去改它，
# 但建議還是設定一個不容易猜到的值。
ADMIN_SECRET=請換成一組自己的英數密碼

# --- 選填設定 ---

# LINE 官方帳號的連結，網站上「加LINE」之類的按鈕會連到這裡。不填的話該按鈕可能不會顯示或無作用。
NEXT_PUBLIC_LINE_URL=https://line.me/R/ti/p/@你的LINE官方帳號ID

# 網站的正式網址（例如你自己買的網域，或 Vercel 給的網址）。
# 不填也沒關係：網站會自動改用 Vercel 提供的正式網址（VERCEL_PROJECT_PRODUCTION_URL）；
# 兩者都沒有的話，Notion「網站連結」欄位的自動回填功能就會直接跳過，不影響其他功能。
NEXT_PUBLIC_SITE_URL=https://your-domain.com
